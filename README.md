# Advanced Java Training

A repository for practicing **advanced Java concepts** through exercises and small projects.  
Topics include OOP, inheritance, collections, exception handling, and JDBC.

## Project Structure

- `model/` – entity and domain classes (e.g., User, BaseEntity)
- `dao/` – data access objects for database operations
- `service/` – business logic classes
- `utils/` – utility/helper classes